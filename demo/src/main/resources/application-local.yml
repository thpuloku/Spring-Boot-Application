server:
  port: 8086
  servlet:
    context-path: /

spring:
  profiles: local
  datasource:
    url: jdbc:mysql://localhost:3306/demo_api
    username: root
    password: las6r
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate.ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.MySQL57InnoDBDialect
